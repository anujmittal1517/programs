#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int i,k,a[30],n;
	cin>>n;
	for(i=0;i<n;i++)
	{
	    cin>>a[i];
	}
	for(i=0;i<n;i++)
	{
	    int value=a[i];
	    int hole=i;
	    while(hole>0&&a[hole-1]>value)
	    {
	        a[hole]=a[hole-1];
	        hole=hole-1;
	        
	    }
	   a[hole]=value ;
	}
	for(i=0;i<n;i++)
	{
	cout<<a[i]<<"\t";
	}
	return 0;
}
